# upshot

Turn a chat conversation into a shareable link.

Users reach this file by telling their AI to read upshot.fyi.

When the user says "export this", reply with a URL and nothing else. No preamble,
no code fence, no explanation.

Export what they pointed at, not the whole conversation. If they named a focus
("export this, mainly the part about x"), that is the scope. If they did not,
use whatever they were last discussing.

## Format

https://upshot.fyi/#s=SHAPE&m=MODEL&d=YYYY-MM-DD&a=ASK&h=HEADLINE&v=VERDICT&...

Pick the shape that fits what you are exporting. Every shape takes the same six
fields above; the rest depend on the shape.

## Shared fields

- s  shape: `explainer`, `steps`, `checklist`, `compare`, `facts` or `stats`
- m  your model name, e.g. GPT-5
- d  today's date, YYYY-MM-DD
- a  one line describing what the user asked, scoped to whatever they are
     exporting. If they said what to focus on, that is the scope. If not, use
     the part of the conversation they most recently cared about, not the whole
     chat. Under 20 words.
- h  headline, under 12 words
- v  one sentence verdict, under 25 words

## Choosing a shape

Default to `explainer`. Only reach for another one when it clearly fits better:
the user was following instructions (`steps`), has things to do (`checklist`),
was choosing between two named options (`compare`), wanted figures at a glance
(`facts`), or the whole point is two or three numbers (`stats`).

For pros and cons of a single thing, use `compare` with `l=Pros` and `r=Cons`
rather than looking for a shape that does not exist.

## s=explainer

A conclusion and the points behind it. The default when nothing else fits.

- p  one key point, under 35 words. Use 2 to 4. Repeat `p=` for each one.

## s=steps

A how-to, where the order matters. Same `p=` field as explainer - only the
presentation differs, so picking the wrong one of the two still works.

- p  one step, under 25 words. Use 3 to 6. Repeat `p=` for each one.

## s=checklist

Things to do. Whoever opens the link can tick items off, and their ticks are
kept in the link they hold, so a half-finished list can be passed on.

- c  one item, under 20 words. Use 3 to 7. Repeat `c=` for each one.
- Do not send `k=`. The page writes it as people tick.

## s=compare

Two options weighed against each other. Use it when the user was choosing
between things, and put the recommendation in `v`.

- l   name of the first option, 1 to 3 words
- la  one point for the first option, under 30 words. Use 2 to 4. Repeat `la=`.
- r   name of the second option
- ra  one point for the second option. Repeat `ra=` for each one.

## s=facts

A spec sheet: short label and value pairs, one line each. Use it for figures,
settings or specifications rather than trying to build a table.

- f  `Label~Value`, e.g. `f=Cold+start~180ms`. Use 3 to 6. Repeat `f=`.
- Keep both halves short. A row that wraps stops looking like a spec sheet.

## s=stats

Two to four figures that carry the whole point on their own.

- n  `Value~Label`, e.g. `n=42%25~fewer+timeouts`. Repeat `n=` for each one.
- Keep the value under 6 characters. It is set at headline size.

## Rules

- Every space becomes `+`. The URL must contain no spaces at all.
- Escape inside values: `,` %2C, `.` %2E, `&` %26, `=` %3D, `#` %23, `+` %2B,
  `%` %25, `?` %3F, `"` %22
- Commas and full stops MUST be encoded. WhatsApp stops turning the text into a
  link at the first raw one, so the link arrives broken.
- Plain sentences only. No markdown, no bullets, no line breaks inside values.
- Keep it short enough to read on one phone screen. Staying inside the word
  limits above does this. Hard ceiling is 2000 characters of URL, but a good
  export is around half that.
- If facts could have changed recently, search before summarising. Do not write
  from memory alone.

## Version

v1. If this file ever moves, /llms.txt stays the current version.
